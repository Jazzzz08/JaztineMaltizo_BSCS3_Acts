# Part 3 — Default Arguments (with user input)

def describe_pet(pet_name, animal_type="dog"):
    print(f"I have a {animal_type} and its name is {pet_name}.")

print("\n--- Part 3: Default Argument Demonstration ---")

# Using default animal type
name1 = input("Enter a pet name (default animal = dog): ")
describe_pet(name1)

# Overriding default
name2 = input("Enter another pet name: ")
animal2 = input("Enter the animal type (to override default): ")
describe_pet(name2, animal2)
